import { Button, Form } from "antd";
import styled from "styled-components";

const StyledFormContainer = styled(Form)`
    width: 800;
`

const StyledPrimaryButton = styled(Button)`
    margin-right: 10px;
`

export { StyledFormContainer, StyledPrimaryButton }